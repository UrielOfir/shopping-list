export const fetchCategoriesFromServer = async () => {
    // const response = await fetch('https://your-api-url/categories');
    // if (!response.ok) {
    //   throw new Error('Network response was not ok');
    // }
    const categories = ["Category 1", "Category 2", "Category 3"]// await response.json();
    return categories;
  };